# Static files — must be served directly, never caught by SPA wildcard
/sitemap.xml      /sitemap.xml      200
/robots.txt       /robots.txt       200
/llms.txt         /llms.txt         200
/sitemap.xsl      /sitemap.xsl      200

# SPA fallback
/*    /index.html   200
