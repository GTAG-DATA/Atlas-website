{
  "name": "atlas-blog-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute atlas-blog --file=./schema.sql",
    "db:seed": "wrangler d1 execute atlas-blog --file=./seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.78.7"
  }
}
